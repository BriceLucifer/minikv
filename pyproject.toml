[project]
name = "minikv-python-tests"
version = "0.1.0"
description = "Python client dependencies for minikv compatibility tests"
requires-python = ">=3.10"
dependencies = [
  "boto3>=1.34",
  "pyarrow>=15",
  "requests>=2.31",
]

[tool.uv]
package = false
